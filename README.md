# golearn

Small Go learning experiments.

- [`msgqueue/`](msgqueue/) — bounded in-memory queue with a worker pool (`sync.Cond`, backpressure via token channel). See [msgqueue/README.md](msgqueue/README.md).
